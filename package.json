{
  "name": "novoriq-orders-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace @novoriq/shared",
    "dev": "npm run build:shared && concurrently \"npm:dev:api\" \"npm:dev:mobile\"",
    "dev:api": "npm run dev --workspace @novoriq/api",
    "dev:mobile": "npm run dev --workspace @novoriq/mobile",
    "test": "npm run build:shared && npm run test --workspace @novoriq/api && npm run test --workspace @novoriq/mobile",
    "build:api": "npm run build:shared && npm run build --workspace @novoriq/api",
    "build:android-apk": "npm run build:android-apk --workspace @novoriq/mobile",
    "lint": "npm run build:shared && npm run lint --workspace @novoriq/api && npm run lint --workspace @novoriq/mobile",
    "postinstall": "npm run build:shared"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": "20.x"
  }
}
